# Sdl3Sharp.ImGui.Native

## Project Overview

This project provides a native C++ library that compiles Dear ImGui with dear_bindings to expose a C-compatible API. Includes SDL3 backends for input handling (`imgui_impl_sdl3`), SDL_Renderer (`imgui_impl_sdlrenderer3`), and SDL_GPU (`imgui_impl_sdlgpu3`).